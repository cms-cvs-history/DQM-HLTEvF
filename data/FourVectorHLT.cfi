# $Id: FourVectorHLT.cfi,v 1.3 2008/06/11 10:01:31 lorenzo Exp $
    module hltResults = FourVectorHLT {
       untracked double ptMin = 0
       untracked double ptMax = 100
       untracked bool plotAll = true
       # this is I think MC
       #InputTag triggerSummaryLabel = "hltTriggerSummaryAOD::HLT"
       # this is data (CRUZET I or II best guess)
       InputTag triggerSummaryLabel = "hltTriggerSummaryAOD::FU"
       VPSet filters = {}
    }
